{
  "name": "@meridian/app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/app/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @meridian/app build"
      },
      "inputs": ["production", "^production"],
      "outputs": ["{projectRoot}/.output"],
      "cache": true
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @meridian/app start"
      },
      "cache": false
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @meridian/app typecheck"
      },
      "inputs": ["default", "^default"],
      "cache": true
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @meridian/app test"
      },
      "inputs": ["default", "^default"],
      "cache": true
    }
  }
}
