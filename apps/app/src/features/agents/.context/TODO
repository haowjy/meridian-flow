- `AgentPicker.tsx`: distinguish same-name entries within one ownership group. The binding replacement browser probe created two Personal Agents with the same name and description but different slugs; their rows were visually indistinguishable despite correct exact selection IDs. Show a meaningful identity discriminator without changing saved selections. Verify in the real picker as part of Milestone 1 readiness.
