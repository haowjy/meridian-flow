# TODO — must-do follow-ups (project shell)

- PR #531: finish shell-first feedback for document-session startup and pre-route
  live-opener waits. `routing/ProjectRouteBoundary.tsx` now implements the address
  wait policy: persistent shell, eligible retained content, otherwise blank for
  approximately 500ms then skeleton, no spinner/loading text/minimum duration.
  Complete local-first acquisition and accepted navigation before deciding where
  session-readiness feedback belongs; do not add another competing loading owner.
  Verify cold replay, missing content cache, offline acquisition, A/B/A supersession,
  explicit empty Editor and wrong-content masking. Route-boundary feedback alone
  does not fix the remote-gated opener. Accepted navigation owns close/reload ordering.

- Complete the local-first ownership cutover tracked in [PR #531](https://github.com/haowjy/meridian-flow/pull/531): proven content readiness, durable resource/intent ownership, concurrent admission and non-destructive migration. [Current architecture](resource-lifecycle-architecture.md) separates implemented navigation from those pending owners. Preserve the existing session/backend foundations; remove displaced writers at activation.
