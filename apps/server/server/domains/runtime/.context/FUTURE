- Measure the auto-spend from a background report waking the parent: the report
  auto-starts a parent turn with no writer action (design open q2). No
  budget/billing numeric assertion records what bounds that turn; measure
  `assertTurnBudget` / `canStartTurn` / ledger behavior for it
  (`apps/server/server/domains/runtime/spawn/child-run-driver.ts`).

- The in-memory thread repository's `nextRef` counter increments outside a
  transaction, so a rolled-back test create leaves a handle gap the Drizzle
  counter (which shares the insert transaction) would not. Test-only divergence;
  converge the adapters only if the in-memory one gains rollback semantics
  (`apps/server/server/domains/threads/adapters/in-memory/repositories.ts`).

- Debug packaged add should resolve launch-agents by coordinate, not scan every system package (`apps/server/server/domains/packages/domain/account-skill-install.ts`).
- Share fuzzy filter / group-heading chrome between composer `/` and manuscript slash (`apps/app/src/components/app/composer/command/`, `apps/app/src/core/editor/extensions/slash/`).
