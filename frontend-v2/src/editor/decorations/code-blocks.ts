// Backward-compat re-export. Inline code moved to inline-code.ts (Phase 3).
// Fenced code handled by block-decorations.ts StateField (Phase 2).
// This file can be deleted once all imports are updated.
export { inlineCodeDecorations as codeDecorations } from "./inline-code"
