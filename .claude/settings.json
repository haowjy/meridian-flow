{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in */backend/migrations/*.sql) ./backend/scripts/lint-migrations.sh \"$f\" ;; esac; }",
            "statusMessage": "Linting migration..."
          }
        ]
      }
    ]
  }
}
