@AGENTS.md

## Claude-specific

Use `explorer` for broad codebase exploration. Never use the generic `claude`
agent type or a fork of yourself.

Anything a writer sees (components, layout, styling, rendering behavior) is
implemented by `frontend-coder`, whoever is leading the work.
