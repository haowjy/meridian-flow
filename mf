#!/bin/sh
# Local dev CLI for this worktree's running stack. Usage: ./mf  (see tools/dev/cli)
root="$(cd "$(dirname "$0")" && pwd)"
exec "$root/node_modules/.bin/tsx" "$root/tools/dev/cli/main.ts" "$@"
