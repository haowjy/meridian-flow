CREATE INDEX "turns_recoverable_created" ON "turns" USING btree ("created_at") WHERE "turns"."role" = 'assistant' AND "turns"."status" IN ('pending', 'streaming', 'waiting_interrupt'); -- migration-lint: skip INDEX_NOT_CONCURRENTLY (release runner applies migrations in one transaction, where CONCURRENTLY is not allowed; this in-flight-turn-only partial index builds in milliseconds at beta size).
