# Model catalog overrides.
# Uncomment and edit the sections below to customize aliases, routing, and visibility.

# [aliases]
# opus = "claude-opus-4-6"
# gpt = "gpt-5.4"

# [metadata.opus]
# role = "default reviewer"
# strengths = "deep review, architecture"

# [harness_patterns]
# claude = ["claude-*", "opus*", "sonnet*", "haiku*"]
# codex = ["gpt-*", "o1*", "o3*", "o4*", "codex*"]
# opencode = ["opencode-*", "gemini*", "*/*"]

# [model_visibility]
# include = []
# exclude = ["*-latest", "*-deep-research", "gemini-live-*", "o1*", "o3*", "o4*"]
# hide_date_variants = true
# max_age_days = 180
# max_input_cost = 10.0
